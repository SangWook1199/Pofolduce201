<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.kosa._musketeers.mapper.AdminMapper">

	<!-- 전체 유저 조회 -->
	<select id="getAllUsers"
		resultType="org.kosa._musketeers.domain.User">
		SELECT * FROM users
	</select>

	<!-- 특정 유저 조회 -->
	<select id="getUserById" parameterType="int"
		resultType="org.kosa._musketeers.domain.User">
		SELECT user_id, email, nickname, password, user_type,
		company_certification, company_name, point,
		sanction_count, sanction_type, sanction_period,
		date,  <!-- 시간 포함한 DATETIME 그대로 -->
		user_image_location
		FROM users
		WHERE user_id = #{userId}
	</select>

	<!-- 제재 기간 설정 -->
	<update id="updateSanction">
		UPDATE users
		SET sanction_period = #{sanctionPeriod},
		sanction_type = #{sanctionType}
		WHERE user_id = #{userId}
	</update>

	<!-- 제재 횟수 증가 -->
	<update id="increaseSanctionCount" parameterType="int">
		UPDATE users
		SET sanction_count = sanction_count + 1
		WHERE user_id = #{userId}
	</update>

</mapper>
